import React from 'react';

export default React.createClass({

    render() {
        // TODO : if user is not logged in, display a button to force him to log in
        return (
            <div>
                Post
            </div>
        );
    }

});
